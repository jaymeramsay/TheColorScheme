'use strict';

console.log('Color Scheme App.js is running!');

var template = React.createElement(
  'p',
  null,
  'Here is some JSX!'
);

ReactDOM.render(template, document.getElementById('app'));
